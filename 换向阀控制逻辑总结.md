# 换向阀控制系统逻辑总结


## 二、系统运行流程

### 2.1 初始化阶段（首次执行）

```cpp
if (is_first_exec == 1) {
    // 重置所有标志
    rev_start_locked = 0;      // 启动锁存清零
    is_phase1_entered = 0;     // 第一阶段标志清零
    is_phase2_entered = 0;     // 第二阶段标志清零
    phase1_current_state = 0;  // 初始逻辑状态：关闭
    force_state = 0;           // 强制状态清零
}
```

### 2.2 启动判断（一次性触发）

**启动条件**：
```cpp
if (bypass_duty <= 25 && (current_oil_P > start_high_P || current_oil_P < start_low_P)) {
    // 进入第一阶段
    rev_start_locked = 1;          // 永久锁存（不可逆）
    is_phase1_entered = 1;         // 进入第一阶段
    phase1_current_state = 1;      // 第一阶段初始逻辑状态：开启
}
```

**启动条件说明**：
- `bypass_duty <= 25%`：旁通阀占空比不超过25%
- `current_oil_P > start_high_P` 或 `current_oil_P < start_low_P`：油压超出启动阈值范围

**启动后状态**：
- 进入第一阶段
- 逻辑状态 = 1（逻辑开启）
- 物理状态 = 0（物理关闭）

---

### 2.3 第一阶段：固定频率切换（不可逆）

**运行条件**：
- `rev_start_locked == 1`（已启动）
- `is_phase1_entered == 1`（已进入第一阶段）
- `is_phase2_entered == 0`（未进入第二阶段）

**切换逻辑**：

| 当前逻辑状态 | 持续时间 | 动作 | 新逻辑状态 | 对应物理状态 |
|------------|---------|------|-----------|------------|
| 1（开启） | ≥ 800ms | 切换到关闭 | 0 | 1（物理开） |
| 0（关闭） | ≥ 1200ms | 切换到开启 | 1 | 0（物理关） |

**代码片段**：
```cpp
// 逻辑开启800ms → 逻辑关闭
if (phase1_current_state == 1 && elapsed_time >= PHASE1_ON_DURATION) {
    phase1_current_state = 0;
    logic_rev_state = 0;
    phys_rev_state = 1;  // 物理开启
}

// 逻辑关闭1200ms → 逻辑开启
else if (phase1_current_state == 0 && elapsed_time >= PHASE1_OFF_DURATION) {
    phase1_current_state = 1;
    logic_rev_state = 1;
    phys_rev_state = 0;  // 物理关闭
}
```

**阶段切换条件**：
```cpp
if (current_oil_P >= user_max_P) {
    is_phase2_entered = 1;  // 进入第二阶段（不可逆）
}
```

---

### 2.4 第二阶段：基于油压的动态切换 + 超时强制机制

**运行条件**：
- `is_phase2_entered == 1`（已进入第二阶段）

#### A. 强制状态处理（优先级最高）

**强制状态类型**：
- `force_state = 1`：强制开启中（持续 `FORCE_ON_DURATION = 1500ms`）
- `force_state = 2`：强制关闭中（持续 `FORCE_OFF_DURATION = 800ms`）
- `force_state = 0`：无强制状态

**强制状态逻辑**：
```cpp
if (force_state != 0) {
    // 强制状态期间：维持当前逻辑/物理状态，不响应油压变化
    // 持续指定时长后退出强制状态
    if (force_elapsed >= target_force_duration) {
        force_state = 0;  // 恢复正常逻辑
    }
}
```

#### B. 正常油压控制逻辑（无强制状态时）

**切换逻辑**：

| 当前逻辑状态 | 油压条件 | 动作 | 新逻辑状态 | 对应物理状态 |
|------------|---------|------|-----------|------------|
| 1（开启） | `≥ user_max_P` | 切换到关闭 | 0 | 1（物理开） |
| 0（关闭） | `≤ user_min_P` | 切换到开启 | 1 | 0（物理关） |
| 任意 | `user_min_P < 油压 < user_max_P` | 维持当前状态 | 不变 | 不变 |

#### C. 超时强制换向机制

**触发条件**：在正常逻辑下，某个状态持续时间过长

| 当前逻辑状态 | 超时时间 | 动作 | 强制状态 |
|------------|---------|------|---------|
| 1（开启） | `≥ on_overtime` | 进入强制关闭 | `force_state = 2`（持续800ms） |
| 0（关闭） | `≥ off_overtime` | 进入强制开启 | `force_state = 1`（持续1500ms） |

**代码片段**：
```cpp
// 逻辑开启超时 → 强制关闭
if (logic_rev_state == 1 && state_elapsed_time >= actual_on_overtime) {
    force_state = 2;  // 强制关闭状态
    logic_rev_state = 0;  // 强制逻辑关闭
    phys_rev_state = 1;   // 物理开启
}

// 逻辑关闭超时 → 强制开启
else if (logic_rev_state == 0 && state_elapsed_time >= actual_off_overtime) {
    force_state = 1;  // 强制开启状态
    logic_rev_state = 1;  // 强制逻辑开启
    phys_rev_state = 0;   // 物理关闭
}
```

**超时保护机制**：
```cpp
int actual_on_overtime = on_overtime < MIN_OVERTIME ? MIN_OVERTIME : on_overtime;
int actual_off_overtime = off_overtime < MIN_OVERTIME ? MIN_OVERTIME : off_overtime;
```
确保超时时间至少为 `MIN_OVERTIME = 100ms`。

---

## 三、辅助功能

### 3.1 频率计算

**计算逻辑**：
```cpp
if (freq_elapsed_time >= FREQ_UPDATE_INTERVAL) {  // 每1000ms更新一次
    float elapsed_minutes = freq_elapsed_time / 60000.0;
    current_freq = (switch_count / 2.0) / elapsed_minutes;  // 每2次切换 = 1个周期
}
```

**说明**：
- `switch_count`：累计切换次数
- 每2次切换 = 1个完整的换向周期（开启→关闭→开启）
- 频率单位：次/分钟

### 3.2 风冷器控制（滞回控制）

```cpp
if (current_oil_T >= on_temp && current_ac_state == 0) 
    ctrl_cooler_enable_1 = 1;  // 开启风冷器

if (current_oil_T <= off_temp && current_ac_state == 1) 
    ctrl_cooler_enable_1 = 0;  // 关闭风冷器
```

**控制策略**：
- 油温 ≥ `on_temp`：开启风冷器
- 油温 ≤ `off_temp`：关闭风冷器
- 滞回控制，避免频繁开关

### 3.3 动态执行周期调整

```cpp
if (is_phase2_entered == 1) 
    AIntervalMs = 10;      // 第二阶段：10ms周期（快速响应）
else if (is_phase1_entered == 1) 
    AIntervalMs = 50;      // 第一阶段：50ms周期
else {
    // 未启动：根据油温与设定温度的差距动态调整
    AIntervalMs = current_diff < temp_diff/4 ? 200 : 
                  (current_diff > temp_diff/2 ? 50 : 100);
}
```

**策略说明**：
- 第二阶段：10ms周期（需要快速响应油压变化）
- 第一阶段：50ms周期（固定频率切换，不需要太快）
- 未启动：根据油温偏差动态调整（温度接近设定值时降低执行频率，节省资源）

---

## 四、系统变量持久化

代码使用系统变量保存关键状态，确保程序重启后状态能够恢复：

### 4.1 阶段控制变量
- `rev_is_first_exec`：首次执行标志
- `rev_start_locked`：启动锁存（不可逆）
- `is_phase1_entered`：第一阶段进入标志（不可逆）
- `is_phase2_entered`：第二阶段进入标志（不可逆）
- `rev_phase1_start_tick`：第一阶段当前状态计时起点
- `rev_phase1_current_state`：第一阶段逻辑状态
- `rev_phase2_state_start_tick`：第二阶段当前状态计时起点

### 4.2 强制状态变量
- `rev_force_state`：强制状态（0=无，1=强制开启，2=强制关闭）
- `rev_force_start_tick`：强制状态开始计时起点

### 4.3 统计变量
- `rev_last_reversal_state`：上一次逻辑状态
- `rev_switch_count`：累计切换次数
- `rev_freq_calc_start_tick`：频率计算起始时间
- `rev_current_freq`：当前频率（次/分钟）

---

## 五、状态转换流程图

```
┌─────────────────────────────────────────────────────────────┐
│                      系统初始化                              │
│  逻辑状态=0（关闭），物理状态=1（开启）                       │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │   等待启动条件                │
        │   bypass_duty≤25% 且          │
        │   油压超出启动阈值            │
        └──────────┬───────────────────┘
                   │
                   ▼
        ┌──────────────────────────────┐
        │   第一阶段：固定频率切换      │
        │   开启800ms ↔ 关闭1200ms      │
        └──────────┬───────────────────┘
                   │
                   ▼ (油压≥最大油压)
        ┌──────────────────────────────┐
        │   第二阶段：动态控制          │
        │                             │
        │   ┌──────────────────────┐  │
        │   │ 正常油压控制逻辑     │  │
        │   │ 高油压→关闭          │  │
        │   │ 低油压→开启          │  │
        │   └──────────┬───────────┘  │
        │              │               │
        │              ▼ (超时)        │
        │   ┌──────────────────────┐  │
        │   │ 超时强制换向         │  │
        │   │ 进入强制状态         │  │
        │   │ （持续指定时长）     │  │
        │   └──────────────────────┘  │
        └──────────────────────────────┘
```

---

## 六、关键参数总结

| 参数名称 | 默认值 | 说明 |
|---------|--------|------|
| `PHASE1_ON_DURATION` | 800ms | 第一阶段逻辑开启时长 |
| `PHASE1_OFF_DURATION` | 1200ms | 第一阶段逻辑关闭时长 |
| `FORCE_ON_DURATION` | 1500ms | 强制开启状态持续时长 |
| `FORCE_OFF_DURATION` | 800ms | 强制关闭状态持续时长 |
| `MIN_OVERTIME` | 100ms | 最小超时时间 |
| `FREQ_UPDATE_INTERVAL` | 1000ms | 频率更新周期 |
| `user_max_P` | 系统变量 | 最大油压阈值 |
| `user_min_P` | 系统变量 | 最小油压阈值 |
| `start_high_P` | 系统变量 | 启动高油压阈值 |
| `start_low_P` | 系统变量 | 启动低油压阈值 |
| `on_overtime` | 系统变量 | 逻辑开启超时时间 |
| `off_overtime` | 系统变量 | 逻辑关闭超时时间 |
| `on_temp` | 系统变量 | 风冷器开启温度 |
| `off_temp` | 系统变量 | 风冷器关闭温度 |

---

## 七、设计特点

### 7.1 安全性设计
- **启动条件**：需要旁通阀占空比≤25%且油压超出阈值，防止误启动
- **阶段切换不可逆**：一旦进入某个阶段，无法返回（状态机设计）
- **超时保护**：防止某个状态持续过久导致系统异常

### 7.2 灵活性设计
- **系统变量配置**：关键参数可通过TSMaster系统变量动态配置
- **动态执行周期**：根据系统状态自动调整执行频率，平衡性能和响应速度

### 7.3 可维护性设计
- **状态持久化**：使用系统变量保存状态，程序重启后恢复
- **详细日志输出**：每个状态变化都有日志记录，便于调试

---

## 八、潜在问题和建议

### 8.1 潜在问题
1. **逻辑状态与物理状态的相反关系**：代码注释中提到，但如果不仔细理解，容易混淆
2. **强制状态持续时间固定**：`FORCE_ON_DURATION` 和 `FORCE_OFF_DURATION` 是宏定义，无法动态调整
3. **超时时间下限**：`MIN_OVERTIME = 100ms` 可能在某些场景下仍然过短

### 8.2 改进建议
1. **添加状态转换验证**：在状态转换时检查逻辑状态和物理状态的一致性
2. **将强制时长参数化**：改为系统变量，可通过TSMaster动态配置
3. **添加状态机图**：在代码注释中添加状态转换图，便于理解

---

## 九、代码执行流程总结

```
每个周期执行流程：
1. 读取系统变量和当前传感器数据
2. 变量有效性验证
3. 首次执行初始化（如果需要）
4. 启动判断（如果未启动）
5. 第一阶段逻辑处理（如果在第一阶段）
6. 第二阶段逻辑处理（如果在第二阶段）
   - 优先处理强制状态
   - 然后执行正常油压控制
   - 检查超时并触发强制换向
7. 频率计算和风冷器控制
8. 动态调整执行周期
9. 保存系统变量（状态持久化）
10. 调试输出
```

---

## 十、关键代码片段说明

### 10.1 状态转换的核心代码
```cpp
// 逻辑状态 → 物理状态转换
logic_rev_state = !phys_rev_state;
phys_rev_state = !logic_rev_state;
ctrl_reversal_valve_enable_1 = phys_rev_state;  // 写入CAN物理状态
```

### 10.2 超时强制换向的核心代码
```cpp
// 检查超时
int state_elapsed_time = sys_current_tick - phase2_state_start_tick;

// 逻辑开启超时 → 强制关闭
if (logic_rev_state == 1 && state_elapsed_time >= actual_on_overtime) {
    force_state = 2;  // 进入强制关闭状态
    force_start_tick = sys_current_tick;
    logic_rev_state = 0;
    phys_rev_state = 1;
}
```

### 10.3 状态持久化的核心代码
```cpp
// 保存系统变量（逻辑状态）
rev_is_first_exec = is_first_exec;
rev_start_locked = rev_start_locked;
rev_phase1_current_state = phase1_current_state;
rev_force_state = force_state;
rev_force_start_tick = force_start_tick;
// ... 其他变量
```

---

**文档版本**：v1.0  
**最后更新**：2025-01-XX  
**适用范围**：TSMaster MiniProgram 换向阀控制逻辑

